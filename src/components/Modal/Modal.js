import React from 'react';
import {View, Text} from 'react-native';

const Modal = () => {
  return (
    <View>
      <Text style={{color: '#222'}}>Modal</Text>
    </View>
  );
};

export default Modal;
